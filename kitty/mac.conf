kitty_mod command
font_size 14

map kitty_mod+ctrl+f    toggle_fullscreen
# there's a bug where the system shortcut doesn't work,
# so we redefine it here
map kitty_mod+º         macos_cycle_through_os_windows

# used with MesloLGS Nerd Font Mono
adjust_line_height  5
